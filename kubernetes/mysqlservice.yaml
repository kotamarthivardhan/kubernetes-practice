apiVersion: v1
kind: Service

metadata:
  name: mysql
  namespace: app-demo

spec:
  type: ClusterIP

  selector:
    app: demo-app-mysql

  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
