apiversion: v1
kind: Service
metadata:
  name: backend-network-service
  namespace: app-demo 
selector:
  type: ClusterIP 
  spec:
    selector:
      app: backend
    ports:
      - protocol: TCP
        port: 5000
        targetPort: 5000