apiVersion: v1
kind: Service   
metadata:
  name: frontend
  namespace: app-demo
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 32428
