apiVersion: v1
kind: PersistentVolume

metadata:
  name: demo-app-mysql-pv

spec:
  capacity:
    storage: 1Gi

  accessModes:
    - ReadWriteOnce

  persistentVolumeReclaimPolicy: Retain

  hostPath:
    path: /data/mysql
